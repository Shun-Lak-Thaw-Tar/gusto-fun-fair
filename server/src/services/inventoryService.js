// Inventory policy is intentionally deferred. This boundary is reserved for the
// team's eventual ticket-limit checking and reservation/release rules.
export const assertInventoryAvailable = async () => true;
